#include <iostream>
#include <math.h>
#include <iomanip>
using namespace std;
int main()
{
	setlocale(LC_CTYPE, " Russian");
	double y;
	double dx = 0.5;
	double r1 = 2;
	cout << setw(28) << "Значения функции" << setw(12) << endl;
	cout << "________________________________________" << endl;
	cout << '|' << setw(10) << 'X' << setw(10) << '|' << setw(10) << 'Y' << setw(10) <<
		'|' << endl;
	cout << "________________________________________" << endl;
	for (double x = -6; x <= 10; x += dx) {
		if (x <= -2) y = (-pow(x+3.5, 2))+2;
		
		if (x > -2 && x <= 2) y = (-1) * sqrt(r1 * r1 - (x ) * (x ));
		if (x > 2 && x <= 8) y = (log10(x) / log10(2))-1 ;
		if (x > 8) y = (-2*x) + 18;
		
		cout << '|' << setw(10) << fixed << setprecision(3) << x << setw(10) << '|' <<
			setw(10) << y << setw(10) << '|' << endl;
		cout << "________________________________________" << endl;
	}
}
